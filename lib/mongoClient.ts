
// MongoDB is disabled. Switched to Supabase.
// Keeping file to prevent import breakages during hot reload, but it does nothing.

export const getCollection = async (collectionName: string) => {
    return null;
};

export const isMongoConfigured = false;
